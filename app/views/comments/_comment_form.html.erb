<%= form_for [book, review, comment], remote: true do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <div id="comment_field">
    <%= f.text_field :content, class: "form-control
      td-text-border", id: "comment-content-#{review.id}" %>
  </div>
<% end %>
