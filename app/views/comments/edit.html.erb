<h1><%= "Edit comment" %></h1>
<%= form_for [@book, @review, @comment] do |f| %>

  <%= f.text_area :content %>
  <%= link_to "Back", root_url, class:"btn btn-danger" %>
  <%= f.submit :Update, class:"btn btn-primary width10" %>

<% end %>
